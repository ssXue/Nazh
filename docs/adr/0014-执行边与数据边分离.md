# ADR-0014: 引脚求值语义二分（PinKind: Exec / Data）

- **状态**: 已实施 Phase 1 + Phase 2 + Phase 3
- **日期**: 2026-04-24（提议）／ 2026-04-28（重写为引脚二分 + Phase 1 落地 + Phase 2 落地）
- **决策者**: Niu Zhihong
- **关联**:
  - 取代本 ADR 原"边二分（EdgeKind）"方案——详细对比与重写理由见
    `docs/superpowers/specs/2026-04-28-pin-kind-exec-data-design.md`
  - 扩展 ADR-0010 Pin 系统（增加 `PinKind` 维度，与 `PinType` 正交）
  - 与 ADR-0011 `NodeCapabilities` 正交（引脚级语义 vs 节点级语义）
  - 为 ADR-0015「反应式数据引脚」铺垫（反应式作为 `PinKind::Reactive` 第三值）
  - 不与 ADR-0012「工作流变量」冲突（变量 = 具名跨工作流共享；引脚二分 = 工作流内按需读最新值）

## 背景

Nazh 当前 DAG 模型里，每条边既是控制流又是数据流——上游 transform 完成 → MPSC send → 下游收到立即 transform。这是一个纯 push 模型，在"每一跳都有意义"的 IO 管道下很自然，但有三类场景反复硌脚：

1. **跨时钟读取**：1 秒主控环的滑动平均，5 分钟摘要环要"最新平均值"——目前只能用变量绕行（setVariable / getVariable 桥接节点污染画布）
2. **派生计算 fan-out**：同一个 Modbus 寄存器读结果，被告警判断 / 仪表盘显示 / 历史采样三个不同节奏消费者使用——push 模型导致重复 IO 或中转节点
3. **UE5 风格组合表达式**：派生字段（华氏温度、距上次告警分钟数、设备序列号）想形成独立"表达式树"挂在主链 Data 输入上，而非串入主流水

## 决策

> 我们决定把"求值语义（push 还是 pull）"放在**引脚**上而非**边**上。`PinDefinition`
> 新增 `kind: PinKind` 字段，与 `PinType`（数据形状）正交。边的语义由两端引脚的
> PinKind 自动派生（Exec 引脚连 Exec 引脚 = push 边；Data 引脚连 Data 引脚 = pull 边）。
> 部署期校验拒绝跨 Kind 连接。

详细设计、4 个核心用例（含 Mermaid 图）、UE5 视觉规范借鉴、与 ADR-0011/0012/0013/0015 关系、Phase 1-5 实施路线图等见 spec 文档：

> `docs/superpowers/specs/2026-04-28-pin-kind-exec-data-design.md`

原"边二分（EdgeKind）"方案的问题（同引脚两条线行为不一致、与 ADR-0010 心智模型脱节、不符合 UE5 已被验证的范式）也在 spec 第一章中详述。

## 可选方案

设计阶段对比了三种放置语义层级的方案（详见 spec 第一章）：

### 方案 A: 边二分（原 ADR-0014）

- **优势**：边类型清晰显式
- **劣势**：同引脚出来两条线行为不同、引擎维度膨胀、UE5 没有此范式

### 方案 B: 节点二分（DATA_SOURCE / EFFECT capability）

- **优势**：贴 SCADA / CQRS 工业范式
- **劣势**：节点级声明无法表达"同节点既给主路径推、又被旁路拉"

### 方案 C: 引脚二分（PinKind: Exec / Data）— 已选

- **优势**：UE5 Blueprint 经过验证、与 ADR-0010 PinType 同层级正交、表达力最强
- **劣势**：引脚维度需要扩展、用户首次需理解形状二分（但形状差异显著，比"两种边"低得多的认知负担）

## 后果

### 正面影响

- 跨时钟读取无需变量桥接节点
- Fan-out 场景设备 IOPS 降为 1× / 触发频率
- 派生表达式与主控逻辑视觉分离
- 后续 ADR-0013 / ADR-0015 / Phase 6 EventBus 直接消费 PinKind 不必重发明语义维度

### 负面影响

- Runner 引入双路径分支（Exec push + Data 写 cache）
- 调试心智需要理解"两种引脚形状"
- 缓存生命周期（空槽兜底、TTL 等）需要后续 Phase 决策

### 风险

- **缓存空槽**：下游被触发时上游还没产生值——Phase 4 通过引脚级 default_value / block_until_ready 策略缓解
- **trace_id 不一致**：一次 transform 可能关联多个上游 trace——CachedOutput 携带 trace_id，观测层接受多 trace 关联
- **Data 边环依赖**：A 拉 B 缓存，B 拉 A 缓存——部署期 detect_data_edge_cycle 拒绝（Phase 1 已实施）

## 实施进度

- ✅ **Phase 1（2026-04-28）**：基础设施骨架——Ring 0 加 `PinKind` + `PinDefinition.kind`；
  `OutputCache` + `CachedOutput`；部署期跨 Kind 校验 + Data 边独立环检测；
  Runner 双路径写入端骨架（cache 写但不读）；ts-rs 导出。生产 14 类节点 0 改动。
  详见 `docs/superpowers/plans/2026-04-28-adr-0014-phase-1-pin-kind-基础.md`。
- ✅ **Phase 2（2026-04-28）**：第一个真实业务节点 + 前端连接期校验 + 画布双端口渲染。
  详见 `docs/superpowers/plans/2026-04-28-adr-0014-phase-2-pin-kind-modbus-真实用例.md`。
  - 后端：`PinDefinition::output_named_data` 工厂；`modbusRead` 新增 `latest`（Json/Data）
    第二输出引脚——Phase 1 双路径骨架在生产节点首次激活；`WorkflowDeployment.output_cache(node_id)`
    访问器（为后续 IPC 打地基）；端到端集成测试覆盖"transform 后 cache 槽被写 + Exec out
    同源 payload"。
  - 跨语言契约：`tests/fixtures/pin_kind_matrix.jsonc`（4 配对穷尽 2×2 笛卡儿积）+ Rust
    `pin_kind_contract.rs` + Vitest `pin-kind-compat.test.ts` 共享同一 fixture；任意一方
    与 fixture 漂移即 CI 红。
  - 前端：`isKindCompatible(from, to)` 与 `isCompatibleWith` 平级；`pin-validator.checkConnection`
    在 PinType 校验前先做 PinKind 闸门，跨 Kind 立即拒（`incompatible-kinds` rejection
    variant + 中文文案"连接不兼容（求值语义不同）..."）；`pin-schema-cache` 加 `resolvePinKind`
    helper；`getPortTooltip` 加"求值语义"行。
  - 画布：`modbusRead` 加入 `useDynamicPort: true` 族，端口 DOM 加 `data-port-pin-kind`
    attribute；CSS `[data-port-pin-kind='data']` 空心冷色边框区分 Exec 实心既有外观。
    Playwright DOM 烟雾测试在 `web/e2e/pin-kind-modbus.spec.ts`。
  - 顺手清债：Phase 1 把 `PinDefinition.kind` 改必填后未回填的 4 处前端 fixture
    （`pin-schema-cache.ts` FALLBACK_SCHEMA + `pin-schema-cache.test.ts` 8 处 + `pin-validator.test.ts`
    helper）一并 `kind: 'exec'` 默认补齐。
- ✅ **Phase 3（2026-04-28）**：UE5 风格 Pure 节点首发——`c2f` / `minutesSince`
  仅声明 Data 引脚（pure-form 由 `nazh_core::is_pure_form` 推导），部署期跳过
  Tokio task spawn；Runner 在 transform 之前调 `src/graph/pull.rs::pull_data_inputs`
  收集 Data 输入：上游若 pure-form 则递归求值（`Box::pin` 处理 async recursion），
  上游若 Exec 节点则读 OutputCache 槽。新 crate `crates/nodes-pure/` 容纳零协议
  依赖的纯计算节点。前端 `isPureForm` helper + 节点 DOM `data-pure-form="true"`
  attribute + CSS 绿头/圆角胶囊视觉差异化（参考 UE5 Blueprint Pure Node）。
  跨语言 fixture `tests/fixtures/pure_form_matrix.jsonc` 4 配对穷尽 Rust + Vitest
  共享。集成测试 `tests/pin_kind_phase3.rs` 用本地 stub 节点验证 source → c2f →
  sink 拉链：sink 收到的 payload 同时含 source Exec push 与 c2f Data pull。
  详见 `docs/superpowers/plans/2026-04-28-adr-0014-phase-3-pure-nodes.md`。
- 🟢 **Phase 4（已实施，2026-04-29）**：Data 输入引脚缓存空/过期兜底策略（`EmptyPolicy`）+
  `BlockUntilReady` Notify+timeout / `DefaultValue(Value)` / `Skip` 三分支；`OutputCache`
  加 `Notify` 唤醒机制 + TTL 过期检查；`PureMemo` per-trace 输入哈希记忆缓存（fan-out
  场景下 pure 节点同一 trace 内只 transform 一次）；跨语言 fixture `empty_policy_matrix.jsonc`；
  前端 pin tooltip 展示 empty_policy / TTL 信息；ts-rs PinDefinition 含新字段。
- 🟡 **Phase 5（规划中）**：视觉打磨（节点头部色按 capability 自动着色）/
  AI prompt 携带 pure-form 信息 / PureMemo trace 完成后清理 / watch channel 替代 Notify。

## 备注

- 本 ADR 是本批次 7 条提案里**改动最大**的一条。Phase 1 通过严格的"骨架先行 + 业务零侵入"策略把改动 contained 在引擎核心，14 类生产节点不需要任何代码修改
- 实施前置条件：ADR-0010 Pin 系统已稳定（边类型系统依赖）、ADR-0011 能力标签已存在
- 未来扩展：`EdgeKind::Reactive`（ADR-0015）作为 PinKind 第三值——本 ADR 的引脚二分让此扩展成为正交维度上的扩值，不是新概念
