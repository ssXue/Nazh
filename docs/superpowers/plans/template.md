# {{功能名}} Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用一句话说明本计划交付什么。

**Architecture:** 用 2-3 句话说明实现路径、边界和依赖。

**Tech Stack:** Rust / Tauri / React / TypeScript / Vitest / Playwright / cargo-deny。

---

## 文件结构

- Modify: `path/to/file`
- Create: `path/to/new-file`
- Test: `path/to/test`

## Task 1: {{任务名}}

**Files:**
- Modify: `path/to/file`
- Test: `path/to/test`

- [ ] **Step 1: 写失败测试或配置校验**

Run:

```bash
command
```

Expected: 说明失败原因或当前缺口。

- [ ] **Step 2: 实施最小变更**

说明精确改动内容。

- [ ] **Step 3: 运行验证**

Run:

```bash
command
```

Expected: 说明应通过的断言。

- [ ] **Step 4: 更新文档**

说明需要同步的 README / AGENTS / crate AGENTS / ADR / RFC / Spec。
