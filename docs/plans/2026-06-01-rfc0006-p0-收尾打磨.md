> **Status:** 已完成（2026-06-01，未提交）

**Goal:** RFC-0006 收尾打磨——解决 5 个落地后暴露的实际使用问题。

**Architecture:** 纯前端改动，涉及 extraction-pipeline / CopilotAttachment / CopilotChatInput / CopilotChatView / CopilotPanel / DeviceModelingPanel + CSS 样式。无 Rust 侧变更，无新 IPC 命令。

**Tech Stack:** TypeScript + React 19 + ai-sdk + Zod

---

## 任务

### P0-3：`generateObject` provider 兼容性处理 ✅

- [x] `extractWithSchema` 的 fallback 增加 JSON 修复（code fence 提取 → JSON 对象/数组提取 → `repairJson` 修复截断括号 + trailing comma）
- [x] fallback 日志输出 `generateObject` 失败原因

### P0-4：简单设备合并抽取 ✅

- [x] `outline.signalCount ≤ 3` 时跳过 Stage 2，直接从 `signalSummaries` 构建 `SignalDetail`
- [x] `PipelineResult` 新增 `stage2Skipped` 字段
- [x] `formatPipelineResult` 中标注跳过提示

### P0-2：大 PDF 预处理 ✅

- [x] `summarizeLongText` 分片策略：按段落边界分片（15k/片），逐片 AI 摘要，合并后仍超限则二次压缩
- [x] `splitIntoChunks` 按段落边界分割
- [x] `summarizeChunk` AI 摘要（保留设备/信号/寄存器信息）

### P0-1：Copilot 附件多文件支持 ✅

- [x] `CopilotAttachment` 新增 `validateAttachments` 批量校验 + `MAX_ATTACHMENT_COUNT=3` + `MAX_TOTAL_SIZE=10MB`
- [x] `CopilotChatInput`：`attachment` → `attachments[]`，`input.multiple = true`
- [x] `CopilotChatView`：state `attachment` → `attachments[]`
- [x] `CopilotPanel.handleSend`：签名改为 `(text, attachments?: CopilotAttachment[])`，逐个处理 PDF/ESI
- [x] CSS：附件栏 `flex-wrap` + hint 文字 + attach 按钮禁用态

### P0-5：设备视图 Copilot 跳转按钮 ✅

- [x] 设备卡片右上角 ✨ 建模按钮 → `sendToCopilot`
- [x] DetailPanel 头部「修复」按钮 → `sendToCopilot`
- [x] `dm-btn--copilot` 样式 + `asset-card__copilot` 样式

---

## 验证

- `tsc --noEmit`：零错误
- `vitest run`：29 文件 269 测试全部通过
