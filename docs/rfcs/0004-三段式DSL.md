# RFC-0004：三段式 DSL——Device / Capability / Workflow

> **状态：** 提议中
> **日期：** 2026-04-30
> **决策者：** ssXue
> **关联：** 偏离-01（三段式 DSL）、偏离-02（Safety Compiler）、偏离-04（设备语义 / 能力抽象）、ADR-0010（Pin 声明系统）、ADR-0013（子图与宏）、ADR-0014（Pin 求值语义二分）、ADR-0019（AI 依赖反转）、RFC-0002（分层内核）、蓝图文档 §6.1 / §6.2 / §6.4 / §7

---

> **后续决策落点（2026-04-30）：** ADR-0021 复评后把本 RFC 中"文本通道 / 画布通道"的探索措辞收敛为"AI 编排控制台生成 DSL，编译后导入画布；画布页是唯一编辑/部署真值源"。本 RFC 仍记录 DSL 设计空间，产品入口与部署汇合以 ADR-0021 为准。
>
> **后续决策落点（2026-05-03）：** ADR-0021 二次复评后，三段式 DSL 被拆成不同生命周期的资产层：Device / Capability 由"设备建模控制台"完成说明书 to 设备资产并版本化；Workflow DSL 由"AI 编排控制台"基于已审查设备 / 能力快照生成。AI 编排不得直接消费未审查说明书。

## 动机

蓝图文档 §7 将三段式 DSL 定义为"产品核心资产"：

1. **Device DSL**——设备 / 信号 / 协议 / 寄存器 / 数据转换的结构化描述
2. **Capability DSL**——将底层操作封装为受约束的设备能力（前置条件 / 副作用 / fallback / 安全等级）
3. **Workflow DSL**——业务状态机 / 触发条件 / 动作序列 / 异常处理的声明式描述

当前项目无自定义 DSL。工作流以 FlowGram JSON AST 透传部署，设备/能力建模完全缺失。Pin 声明系统（ADR-0010）部分覆盖信号/端口描述，但远未到 `DeviceSpec` / `CapabilitySpec` / `SignalSpec` 层级。

**问题**：没有设备模型和能力抽象，AI 无法从说明书生成结构化工作流；没有声明式状态机，工作流全靠画布拖拽——无法版本化、无法静态校验、无法离线仿真。蓝图定位"DSL 是核心资产"，当前核心资产缺失。

**目标**：设计一套与现有 Ring 0/1 架构兼容的三段式 DSL，使其成为引擎的"编译输入"——经过 Safety Compiler 校验后，编译为可部署的 DAG。

---

## 需求与约束

### 硬需求

1. **声明式**——三种 DSL 均为声明式（YAML / TOML / JSON），不含命令式逻辑
2. **可编译**——Workflow DSL 经 Safety Compiler 校验后，编译为引擎可部署的 DAG（`WorkflowGraph` + 节点注册）
3. **类型安全**——单位、量程、信号类型在编译期校验，不推迟到运行时
4. **Ring 纯净性**——DSL parser / compiler 作为 Ring 1 或独立 crate，不进入 `crates/core/`
5. **单部署通道**——最终部署仍由 FlowGram 画布页发起；DSL 编译产物先导入画布，画布确认后再走 `WorkflowGraph` 部署管道
6. **AI 可生成但需分层审查**——AI 可从说明书 / 点表生成 Device / Capability 草稿，但必须人工审查并固化为资产；AI 编排只能基于已审查设备 / 能力快照生成 Workflow DSL

### 可协商

- DSL 具体格式（YAML vs TOML vs 自定义语法）
- Capability DSL 与 Device DSL 是否合并为同一文件
- `stateMachine` 节点的 config schema、timeout 语义和 action route 细节
- 编译输出的中间表示（IR）是否暴露为独立 crate

### 非目标（本 RFC 不覆盖）

- Safety Compiler 的 17 项校验规则细节（另开 ADR）
- AI 生成管道（说明书 → DeviceSpec → CapabilitySpec）的具体 prompt / chain 设计
- 仿真器 / 回放（偏离-05）
- edge-daemon 独立运行时（偏离-03）

---

## 设计

### 整体架构

```
                    ┌─────────────────────┐
                    │  设备建模控制台       │
                    │  说明书 / 点表 / 协议  │
                    └─────────┬───────────┘
                              │ AI 抽取 + 人工审查
                    ┌─────────▼───────────┐
                    │  设备资产库           │
                    │  Device / Capability │
                    │  versioned snapshot  │
                    └─────────┬───────────┘
                              │ 选择资产版本
                    ┌─────────▼───────────┐
                    │  AI 编排控制台        │
                    │  Workflow DSL        │
                    └─────────┬───────────┘
                              │ 编译
                    ┌─────────▼───────────┐
                    │  DSL Compiler        │  ← Ring 1 新 crate
                    │  (parser + checker)  │
                    └─────────┬───────────┘
                              │ Safety + WorkflowGraph JSON
                    ┌─────────▼───────────┐
                    │  FlowGram 画布确认     │
                    └─────────┬───────────┘
                              │ deploy_workflow
                    ┌─────────▼───────────┐
                    │  现有 DAG 部署管道     │
                    └─────────────────────┘

    画布部署通道（唯一）：FlowGram 编辑态 → WorkflowGraph → deploy_workflow
    DSL 生成通道：设备资产快照 + Workflow DSL → Compiler → Safety → WorkflowGraph JSON → 导入画布
```

部署管道只看同一个 DAG schema（`WorkflowGraph`），不需要知道画布内容是人工拖拽产生，还是由 DSL 编译后导入画布。

### 资产生命周期

三段式 DSL 的三个部分不再共享同一个生命周期：

| 层级 | 来源 | 生命周期 | 主要消费者 |
|------|------|----------|------------|
| Device DSL / Signal DSL | 说明书、点表、协议文档、人工补充 | 项目 / 环境级持久资产，版本化保存 | Capability DSL、AI 编排上下文、连接测试 |
| Capability DSL | 设备信号、底层写操作、人工安全约束 | 项目 / 环境级持久资产，跟随设备资产版本演进 | Workflow DSL、Safety Compiler、AI tool catalog |
| Workflow DSL | 自然语言业务目标 + 已审查设备 / 能力快照 | 工作流级源文件，导入画布后成为历史快照 | DSL Compiler、Safety Compiler、画布导入 |

这意味着"说明书 to 设备"是独立建模流程，而不是 AI 编排时的临时上下文。AI 编排若发现设备信息缺失，应提示用户回到设备建模控制台补齐并生成新资产版本。

### 7.1 Device DSL

描述设备实体、信号、协议连接和数据转换。

```yaml
# device.yaml
device:
  id: hydraulic_press_1
  type: hydraulic_press
  manufacturer: "某液压"
  model: YP-320T
  connection:
    type: modbus-tcp          # 复用 ConnectionManager 的协议名
    id: press_modbus          # 引用连接配置中的 connection_id
    unit: 1

signals:
  pressure:
    type: analog_input        # 模拟量输入
    unit: MPa
    range: [0, 35]
    source:
      register: 40001         # Modbus 寄存器地址
      access: read
      data_type: float32
    scale: "raw * 35 / 65535" # 可选缩放表达式

  position:
    type: analog_input
    unit: mm
    range: [0, 150]
    source:
      register: 40003
      access: read
      data_type: float32

  servo_ready:
    type: digital_input
    source:
      register: 40100
      access: read
      bit: 0

  emergency_stop:
    type: digital_input
    source:
      register: 40100
      access: read
      bit: 1

  target_position:
    type: analog_output
    unit: mm
    range: [0, 150]
    source:
      register: 40010
      access: write
      data_type: float32

alarms:
  over_pressure:
    condition: "pressure > 34"
    severity: critical
    action: emergency_stop
```

**关键类型映射**（Rust 侧）：

```rust
/// Device DSL 编译后的结构化设备模型
struct DeviceSpec {
    id: String,
    device_type: String,
    connection: ConnectionRef,          // 引用 ConnectionManager 中的连接
    signals: Vec<SignalSpec>,
    alarms: Vec<AlarmSpec>,
}

struct SignalSpec {
    id: String,
    signal_type: SignalType,            // AnalogInput / AnalogOutput / DigitalInput / DigitalOutput
    unit: Option<String>,
    range: Option<(f64, f64)>,
    source: SignalSource,               // Register / Topic / SerialCommand
    scale: Option<String>,              // Rhai 表达式
}

enum SignalType {
    AnalogInput,
    AnalogOutput,
    DigitalInput,
    DigitalOutput,
}

struct AlarmSpec {
    id: String,
    condition: String,                  // Rhai 表达式
    severity: AlarmSeverity,
    action: Option<String>,
}
```

### 7.2 Capability DSL

将底层寄存器/信号操作封装为受约束的设备能力。AI 和 Workflow DSL 不直接操作寄存器，而是调用能力。

```yaml
# capability.yaml
capability:
  id: hydraulic_axis.move_to
  device: hydraulic_press_1
  description: "控制液压轴移动到指定位置"

  inputs:
    position:
      unit: mm
      range: [0, 150]
      required: true

  outputs:
    - position_reached: bool

  preconditions:
    - "servo_ready == true"
    - "emergency_stop == false"
    - "pressure < 32"

  effects:
    - "axis_state = moving"

  implementation:
    type: modbus-write               # 底层实现方式
    register: 40010                  # 写入 target_position 寄存器
    value: "${position}"             # 参数替换

  fallback:
    - hydraulic_axis.stop

  safety:
    level: high                      # high / medium / low
    requires_approval: false
    max_execution_time: 30s
```

**关键类型映射**：

```rust
struct CapabilitySpec {
    id: String,
    device_id: String,
    inputs: Vec<CapabilityParam>,
    outputs: Vec<CapabilityOutput>,
    preconditions: Vec<String>,         // Rhai 表达式列表
    effects: Vec<String>,
    implementation: CapabilityImpl,     // ModbusWrite / MqttPublish / SerialCommand / Script
    fallback: Vec<String>,              // 引用其他 capability id
    safety: SafetyConstraints,
}

struct CapabilityParam {
    id: String,
    unit: Option<String>,
    range: Option<(f64, f64)>,
    required: bool,
}

struct SafetyConstraints {
    level: SafetyLevel,                 // High / Medium / Low
    requires_approval: bool,
    max_execution_time: Option<Duration>,
}
```

### 7.3 Workflow DSL

描述业务状态机——状态、转移条件、触发动作、异常处理。

```yaml
# workflow.yaml
workflow:
  id: auto_pressing_cycle
  description: "自动压装循环"
  version: "1.0.0"

  devices:
    - hydraulic_press_1               # 引用 device.yaml

  variables:
    target_pressure: 25.0             # MPa
    hold_time: 5.0                    # seconds
    approach_position: 100.0          # mm

  states:
    idle:
      entry: []
      exit: []

    approaching:
      entry:
        - capability: hydraulic_axis.move_to
          args:
            position: "${approach_position}"

    pressing:
      entry:
        - capability: hydraulic_axis.apply_pressure
          args:
            target: "${target_pressure}"

    holding:
      entry:
        - capability: hydraulic_axis.hold_pressure
          args:
            target: "${target_pressure}"
            duration: "${hold_time}"

    returning:
      entry:
        - capability: hydraulic_axis.move_to
          args:
            position: 0.0

    fault:
      entry:
        - capability: hydraulic_axis.stop
        - action: alarm.raise
          args:
            message: "压装循环异常停机"

  transitions:
    - from: idle
      to: approaching
      when: "start_button == true"

    - from: approaching
      to: pressing
      when: "position >= approach_position"

    - from: pressing
      to: holding
      when: "pressure >= target_pressure"

    - from: holding
      to: returning
      when: "hold_timer >= hold_time"

    - from: returning
      to: idle
      when: "position <= 1.0"

    - from: "*"
      to: fault
      when: "pressure > 34"
      priority: 100

  timeout:
    pressing: 60s
    holding: 30s
    on_timeout: fault
```

**编译目标**：Workflow DSL 编译后生成一个 `WorkflowGraph`，其中：
- 状态机整体映射为 `stateMachine` 节点（ADR-0021），循环留在节点内部，不进入外层 DAG 边
- 每个 `transition` 映射为 `stateMachine` config 中的 transition 规则；entry / exit / transition action 映射为 action route port
- 每个 `capability` 调用映射为设备操作节点（新节点类型或脚本节点调用）
- `variables` 映射为 `WorkflowVariables`（复用 ADR-0012）

### Crate 归属

```
crates/
  dsl-core/            # Ring 1 — DeviceSpec / CapabilitySpec / WorkflowSpec 类型 + YAML parser
                      #   依赖: serde, serde_yaml, nazh_core (仅类型引用)
  dsl-compiler/        # Ring 1 — WorkflowSpec → WorkflowGraph 编译器
                      #   依赖: dsl-core, serde_json
```

**Ring 纯净性保证**：
- `crates/core/` 不引入 `serde_yaml`、不引入 DSL 类型
- `DeviceSpec` / `CapabilitySpec` / `SignalSpec` 等类型定义在 `dsl-core`，不进 `crates/core/`
- 在 `WorkflowGraph` 类型尚未独立成 schema crate 前，编译器输出符合 `WorkflowGraph` JSON 契约的 DTO / `serde_json::Value`，用 `WorkflowGraph::from_json()` conformance 测试守护漂移；不反向依赖 `nazh-engine`

---

## 与现有系统的关系

| 现有系统 | DSL 如何复用 / 扩展 |
|----------|---------------------|
| ADR-0010 Pin 声明 | `SignalSpec` 的 `signal_type` / `unit` / `range` 与 Pin 类型系统对齐；编译器生成节点时自动填充 Pin 声明 |
| ADR-0012 工作流变量 | Workflow DSL 的 `variables` 直接编译为 `WorkflowVariables` 初始值 |
| ADR-0013 子图 | 可用于 action group / 可复用宏；状态机循环不直接展开为子图回边，外层 DAG 仍无环 |
| ADR-0014 PinKind | capability 调用节点区分 Exec pin（触发执行）和 Data pin（参数传入） |
| ADR-0019 AiService | 设备建模控制台和 AI 编排控制台都复用现有 `AiService` trait |
| ConnectionManager | Device DSL 的 `connection` 字段引用 `ConnectionManager` 中的连接 ID |
| FlowGram JSON AST | 保持不变；DSL 编译产物先导入画布，由画布页作为唯一部署入口 |

---

## 备选方案考虑

### 方案 A：三段式 YAML DSL（本提案）

三份独立 YAML 文件，各有专属 schema，编译器分阶段处理。

- **优势**：与蓝图 §7 完全对齐；关注点分离清晰；设备模型可跨工作流复用；AI 生成粒度自然（先生成设备，再生能力，最后组装工作流）
- **劣势**：三份文件增加管理复杂度；跨文件引用需要校验；初期学习成本

### 方案 B：单一统一 DSL

将 Device / Capability / Workflow 合并为一个 `.nazh` 文件，类似 Dockerfile。

- **优势**：单文件自包含；分发简单
- **劣势**：设备模型无法跨工作流复用；文件膨胀后难以维护；与蓝图三段式定位不符

### 方案 C：基于现有 FlowGram AST 扩展元数据

不引入新 DSL，而是在 FlowGram JSON AST 上增加 `device` / `capability` 元数据节点。

- **优势**：零新增 crate；画布编辑器可直接操作
- **劣势**：AST 不是人可读/可写的；AI 生成 JSON AST 比 YAML 困难；无法做声明式状态机；与蓝图定位冲突

### 方案 D：Rhai 脚本作为 DSL

用 Rhai 脚本描述设备和工作流，复用现有脚本引擎。

- **优势**：已有运行时支持；表达式能力强
- **劣势**：Rhai 是命令式语言，不是声明式 DSL——无法做静态校验（蓝图 §6.4 的 Safety Compiler 前提是声明式输入）；失去了"可编译、可校验"的核心价值

**推荐方案 A**。理由：蓝图明确要求三段式分离；AI 生成管道需要分阶段输出（设备 → 能力 → 工作流）；声明式是 Safety Compiler 的前提。

---

## 实施拆解

### Phase 0：类型定义（~3 天）

- [ ] 新建 `crates/dsl-core/`，定义 `DeviceSpec` / `SignalSpec` / `AlarmSpec` / `CapabilitySpec` / `WorkflowSpec` 类型
- [ ] YAML parser（`serde_yaml` 反序列化）
- [ ] 单元测试覆盖：合法 / 非法 DSL 文件解析
- [ ] 依赖：`nazh_core`（仅 Ring 0 类型引用）+ `serde` + `serde_yaml`

### Phase 1：设备建模控制台 + Device DSL（~5 天）

- [ ] 说明书 / 点表 / 协议文档导入与来源记录
- [ ] AI 抽取 `DeviceSpec` / `SignalSpec` 草稿，保留字段置信度与来源追溯
- [ ] 设备资产版本化保存（Project / Environment 作用域）
- [ ] `DeviceSpec` → 设备引用现有 `ConnectionDefinition.connection_id`
- [ ] `SignalSpec` → Pin 声明自动生成（对接 ADR-0010）
- [ ] 集成测试：加载 / 保存设备资产版本 → 验证 `DeviceSpec` 结构正确

### Phase 2：Capability DSL 资产化（~5 天）

- [ ] 基于设备信号与写操作生成 / 编辑 `CapabilitySpec`
- [ ] `CapabilitySpec` → 版本化能力目录（与设备资产版本绑定）
- [ ] 前置条件编译为 Rhai 表达式
- [ ] `implementation` 字段映射到底层协议操作
- [ ] 集成测试：加载能力目录版本 → 验证参数校验 / 前置条件求值

### Phase 3：Workflow DSL 编译器（~7 天）

- [ ] 选定设备资产 / 能力目录版本 + `WorkflowSpec` → `WorkflowGraph` JSON 契约编译器
- [ ] `states` / `transitions` → `stateMachine` 节点 config（ADR-0021）
- [ ] entry / exit / transition action → action route port + 下游无环 action DAG
- [ ] `variables` → `WorkflowVariables` 初始值
- [ ] `capability` 调用 → 设备操作节点生成
- [ ] 集成测试：加载设备资产快照 + Workflow DSL → 编译 → `deploy_workflow` → 验证执行

### Phase 4：AI 生成管道对接（~5 天，依赖 Phase 1-3）

- [ ] 设备建模控制台：说明书文本 / 点表 → Device / Capability DSL patch proposal
- [ ] AI 编排控制台：设备资产快照 + 自然语言目标 → Workflow DSL patch proposal
- [ ] 结构化输出约束（`uncertainties` / `warnings` 字段）
- [ ] 前端 DSL 编辑器（YAML 编辑 + 只读资产快照 + 编译反馈）

### Phase 5：Safety Compiler 对接（后续 ADR）

- [ ] 单位一致性校验
- [ ] 量程边界校验
- [ ] 前置条件可达性校验
- [ ] 状态机完整性校验（不可达状态 / 无出口状态 / 循环触发）
- [ ] 危险动作审批校验
- [ ] 机械互锁校验

---

## 风险与未知

1. **Workflow DSL → WorkflowGraph 编译复杂度**：ADR-0021 已选择 `stateMachine` 节点 + 无环 action DAG，但 transition config、timeout tick、action route 与 source-map 仍需实施细化
2. **与 FlowGram 画布的同步边界**：ADR-0021 已选择 DSL → 画布单向导入；画布编辑后 DSL 成为历史快照。后续若要从画布重新生成 DSL，视为新的 AI 编排会话
3. **Rhai 表达式复用边界**：`preconditions` / `scale` / `condition` 用 Rhai 表达式，需确定求值上下文（哪些变量可访问、sandbox 限制）
4. **Device DSL 与 ConnectionManager 耦合**：当前连接配置是前端 JSON 文件直写。Device DSL 引入后，连接配置是否也从 DSL 生成？还是保持双轨？
5. **性能**：大量设备的 YAML 解析 + 编译开销。初期不考虑，后期按需优化（缓存编译结果）

---

## 未解决的问题

- DSL 文件的存放位置（项目目录结构？数据库？）——依赖 RFC-0003 边缘存储层
- Capability 的 `implementation` 字段是否需要支持组合能力（一个 capability 由多个底层操作组成）
- Workflow DSL 的 `timeout` / `retry` 策略是否在 DSL 层声明，还是由 ADR-0016 边级可观测性覆盖
- 是否需要一个 `dsl-lsp` crate 为编辑器提供补全/校验（远期）
